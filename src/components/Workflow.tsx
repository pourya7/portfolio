import React from 'react';
import {Flex} from '@chakra-ui/core';

const Workflow: React.FC = () => (
  <Flex bg='green.300' width='100%' minHeight='100vh' px={[2, 8, 8, 64]} py={8}>
    Featuring Workflow
  </Flex>
);

export default Workflow;
